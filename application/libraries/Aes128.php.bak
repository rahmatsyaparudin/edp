<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

require_once 'Aesencryption.php';

class Aes128
{
    function aesEncrypt($data)
    {
        $inputKey = "qwertyuiop123457";
        $blockSize = 128; 
        $setIV = "";
        $mode = "M_ECB";
        $aes = new Aesencryption($data, $inputKey, $blockSize, $mode, $setIV);
        $encrypt = $aes->encrypt();
        return $encrypt;
    }

    function aesDecrypt($data)
    {
        $inputKey = "qwertyuiop123457";
        $blockSize = 128; 
        $setIV = "";
        $mode = "M_ECB";
        $aes = new Aesencryption($data, $inputKey, $blockSize, $mode, $setIV);
        $decrypt = $aes->decrypt();
        return $decrypt;
    }
}
